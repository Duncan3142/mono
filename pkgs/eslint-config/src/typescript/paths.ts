import type { Path } from "#core"

const TS_CONFIG_DEFAULT: Path = "tsconfig.json"

// eslint-disable-next-line import-x/prefer-default-export -- Expect more exports in the future
export { TS_CONFIG_DEFAULT }
